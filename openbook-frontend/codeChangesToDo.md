1. For all the dialogs inside the flashcards use the UI store to control the UI
- update this 
2. In RightSidebar.tsx we are calling the API in the component instead of the openBookStore.ts
- update all the functions in handle generate and other small changes
